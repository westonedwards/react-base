// Main component JS file --- App
import React from 'react';
import ReactDOM from 'react-dom';

// Styles
import 'normalize.css'; // CSS normalizer and reset

export default class App extends React.Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}